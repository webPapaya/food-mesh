#task which are run on the server

load 'deploy'
# Uncomment if you are using Rails' asset pipeline
load 'deploy/assets'
#load 'config/deploy' # remove this line to skip loading any of the default tasks

task :display_free_disk_space do
  run "df -h"
end
