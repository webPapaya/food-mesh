<%= form_for(@single_page) do |f| %>
  <% if @single_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@single_page.errors.count, "error") %> prohibited this single_page from being saved:</h2>

      <ul>
      <% @single_page.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
